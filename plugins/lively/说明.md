# 活泼模式（群聊接话） 插件

自动读取所在群聊消息，间隔冷却后主动接一句话活跃群气氛（带防刷屏与防二次回复）。关闭=仅在被 @ 时回复群聊。

## 说明

开关：config.json 的 qq_lively_enable（false/true）
附属：qq_lively_interval 接话间隔分钟（1~120，默认 15）
实现位置：qq/qq_bridge.py（_lively_tick 等）。
设置页也有同款开关（QQ 配置区），两处同步同一配置。
