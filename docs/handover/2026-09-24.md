# 交接记录 2026-09-24（DeepSeek Harness 会话 · 丛雨项目）

> 规范见同目录 [`README.md`](README.md)。本次覆盖 2026-09-22 晚 ~ 2026-09-24 的工作。
>
> **本次交付版本：V1.17.2**（README「最新版本」章节已补 `V1.17.2` 条目）。
> 「项目版本」的真正锚点是 **`config.example.json` 的 `wechat_bot_agent` = `AIpet/1.17.2`**
> —— 安装器显示版本（`tool/pack/installer_main._app_version()`）就是读它；
> 另有三处代码默认值同步（`wechat/ilink_client.DEFAULT_BOT_AGENT`、
> `wechat/wechat_bridge` 的 `bot_agent` 默认参数、`run_wechat.py` 的兜底），见 §15.1。
> ⚠ 用户本机 `config.json`（忽略入库的运行配置）里仍是旧值，**没动** —— 要同步说一声。

## 0. 一句话

把融合后的 `main` 做了两整天的清理与打磨：删掉约 2900 行零引用死代码、修掉一批
「看着是开、其实没生效」的配置与界面 bug、重做启动器设置页的开关与枚举控件、
修好 QQ 桥接连不上的 TypeError、把 NapCat 从 4.18.14 升到 4.18.28、
给诺瓦做了头 / 脸 / 胸触摸区域并补上「右键 → 输入对话」；
后半天又在用户逐项拍板后做了设置页控件改造、把 56 处原生弹窗 + 8 处原生输入框换成
主题对话框、用新写的扫描器把配置键默认值全项目对齐、「启动 QQ」加上版本自检；
最后依据用户实测日志修掉了语音识别"开关开着却用不了"的老问题。

**66 个本地提交，全部未推送**（用户要求这几天不碰远程）。

> ⚠ 用户明确否掉的一次改动（教训）：我把提示词页改成「卡片分组 + 状态行 + 固定底部按钮」，
> 用户反馈「不好看，用原来的」→ 已在 `81bebf3` 回退，并写成守护测试。
> **这一页就保持最简单的直排**，别再"美化"。

## 1. 本次目标

用户原话：

> 先不用，依旧不打包，不推送；逐步改进 AIpet-Murasame 融合后代码库（main）：
> 先熟悉架构，再清除冗余、完善代码；重点提升 PCL 启动器的交互质量
> （便捷、简单、清晰 —— 当前偏"只有美观"），分阶段小步改动，每步不破坏现有功能。

## 2. 改了什么

### 2.1 结构清理与死代码（起点）

| 提交 | 改了什么 | 为什么 |
| --- | --- | --- |
| `fd1177c` | `pcl_launcher` 阶段 0：删零引用死代码（-2899 行） | 融合分支带进来一批没人调用的函数，读代码时全是干扰 |
| `809aab8` | 入口收敛到 `silicon_window.launch()`，清死代码 | 多入口互相调用，改一处漏一处 |
| `f1660f7` | 后端零引用死代码（-19 个函数）+ 修一处重复落盘实现 | 同一件事两处写盘会互相覆盖 |
| `93372c7` | 清掉两个孤儿 import | — |

### 2.2 配置与开关（"开关必须真的生效"）

| 提交 | 改了什么 | 为什么 |
| --- | --- | --- |
| `5577f23` | 短语音 / 长语音各自的「启用」与「GPU 加速」开关（默认全开） | 原来只有笼统的开关 |
| `15e9eb3` | 「启用短语音」关掉时不启动 GPT-SoVITS 服务 | 否则白占几 GB 显存 |
| `1386d8b` | 「长语音开关」合并进既有 `longtext_enabled`，不新加键 | 同一件事不许有两个键 |
| `8b7cb33` | 删掉 3 个「文档在宣传、代码从不读」的全局键 | 文档与实现不一致 |
| `683a67d` | CUDA 版本匹配别再把「低版本 torch」判成不匹配 | 否则白下几个 GB |
| `f88891a` | CUDA 版 torch 安装失败时降级继续 | 不让桌宠因此起不来 |
| `7b1b098` | 修复被误删的 Live2D 调参面板 + 4 处未定义名字 + 真正生效的显卡加速开关 | 前一次清理误伤 |
| `4a37e97` | 协作者插件默认全部停用 | 默认关，要用自己去插件页开 |

### 2.3 运行稳健性

| 提交 | 改了什么 | 为什么 |
| --- | --- | --- |
| `afdfab5` | 视频背景解码线程改守护线程 | 修「退出时整个进程崩溃」 |
| `52e410a` | 插件页构建 2209ms → 201ms（去掉构建期里的 PowerShell 调用） | 打开插件页明显卡顿 |
| `93dfe9b` | 修 `build_launcher` 的模块级 NameError（+ 补一个能抓这类错的检查器） | 打包器一启动就崩 |
| `46a6a4b` | `/control` 不再刷屏 + Live2D 对话框字号回到 v1.16.1 | uvicorn 0.37 强制 ProactorEventLoop，老的事件循环切换无效，改成内置 access_log=False + 异常处理器 |
| `8413a8a` | 入口脚本补 stdout 守卫 | 重定向输出时 emoji 直接把启动脚本打断 |

### 2.4 启动器界面（本次重点）

| 提交 | 改了什么 | 为什么 |
| --- | --- | --- |
| `69c91ee` | 设置改为「改动即存」，去掉认不出来的悬浮保存按钮 | 修 P0 丢数据 |
| `49002b1` | 滚轮不碰数值 / 设置页分类拆分 / 布尔滑块统一左假右真 | 用户报的三处 |
| `5d64e13` | 设置页 23 个开 / 关项从滑块改成开关 | 滑块看不出开还是关 |
| `6fe6a25` | API Key 默认打码 + 显示 / 隐藏按钮 | 截图 / 演示会漏 Key |
| `7a583e4` | 插件页「启用」开关样式其实从未生效（被下一行覆盖） | 开关看不出状态 |
| `7fb2610` | 记忆页预览框不再是空白大白框（加占位说明 + 进页自动预览第一份） | — |
| `0bb23a7` | 桌宠卡片按钮配色：一行里只留一个红 | 满行红按钮，看不出哪个是危险操作 |
| `3d37b1a` | 首页控制面板：开关显示真实状态、开关与动作分开、没跑时置灰 | — |
| `94578ea` / `1828274` | 我先加了「开始使用」引导卡 → 用户判定"弄巧成拙"，首页改回原来的样子 | 教训：往界面加整块东西要先问（已写进规范第 8 节） |
| `8cd582c` | 「按住说话」给即时反馈，并在语音识别关着时说清原因 | 原来按下去毫无反应 |
| `769dde6` | 「启动 QQ」先拉起 NapCat 并等扫码、WebUI 同理；弹窗换成可读的主题对话框 | 用户报按钮没反应 |
| `2fd3d72` | 触摸互动改成逐角色能力：没做过的不开，做过的可开可关 | 之前给所有角色盖了 14 个默认触摸区，满身乱响 |
| `d82fbc6` | 切主题立刻换背景（不再需要点「刷新」），旧视频不再残留 | 用户报"必须点刷新" |
| `90d21e8` | 主题页配色预览色块不再被整页透明化压淡 | 同上，色块变淡 |
| `1828274` | 修切主题时 `PCLThemesPanel has been deleted` 报错刷屏 | 延迟回调打到已销毁控件 |
| `570481e` | 诺瓦触摸区域（头 / 脸 / 胸）+ 右键菜单加「💬 输入对话（打字）」 | 用户点名要做 |
| `fa4a230` | 设置页 5 个枚举项从滑杆改成一排按钮（多选一）；`_make_transparent()` 认得 0–255 的 alpha 写法；`main.py` 的 `live2d_enabled` 默认值改成 `false` | 用户点名「设置多选」+ 清掉两个"显示与真实状态不一致" |
| `b59c12f` | 提示词页视觉整理：卡片分组、状态常显（文件名 · 字数 · 已保存 / 未保存）、主次按钮分开、操作按钮固定在底部不随内容滚动；次要按钮样式收敛成 `outline_btn_qss()` 一处定义 | 用户批准的第一页；原布局会把「保存」挤出可视区，两个按钮长得一样 |
| `ad35a9f` | 用户可见的原生弹窗 → 主题对话框（第一批 27 处：总览页 9 + 桌宠 / 记忆页 18）；`MessageDialog` 支持第二个按钮与危险色，新增 `confirm()` | 用户批准：只换看得见的那批，崩溃类（critical）保留原生 |
| `120ba6e` | 弹窗迁移第二批 29 处（主题页 14 + 插件页 10 + 新建角色向导 5）；`page_msg` / `page_confirm` 收敛到 `silicon_dialog` 单一实现 | 同上；现在全项目只剩 1 处故意保留的 `critical` |
| `f247e70` | 原生输入框也换掉：新增主题 `InputDialog` / `ask_text`，迁移 8 处 `QInputDialog.getText`（照片描述 / 姓名 / 关系 / 桌宠 ID 与名称 / 主题重命名 / 触摸部位 ×2） | 弹窗迁移最后一块；白底系统输入框在深色主题下最割裂 |
| `d000853` | 配置默认值对齐：`screen_type` / `model_type` / `longtext_model` / `camera_interval` / `longtext_max_history_turns` / 设置页两个数字框，6 处改为与 `config.example.json` 一致 | 与 `live2d_enabled` 同一类 bug：键缺失时界面显示与引擎实际行为不一致 |
| `6f0d5fd` | 新增 `tool/napcat_version.py`（版本锚解析 / 逐段版本比较 / `check()`）；「启动 QQ」时自检并提示，打包检查改为共用同一份逻辑；README 补一句 | 用户点名：以前只有打包脚本会核对版本，直接启动时 NapCat 被自动升级没人提醒 |
| `d6604f7` | 用户点了没反应的地方补反馈：记忆「备份」三态（没有可备份 / 成功带路径 / 失败）、「清除」失败、插件设置「保存」失败且不关窗口、插件页 / 主题页 / 总览页「打开目录」失败 | AST 审计扫出 30 处静默 except，挑了用户会直接踩到的 6 处 |
| `81bebf3` | **提示词页回退成原来的样子**（用户："不好看，用原来的"）；守护测试改为"防我改回去" | 教训写进规范：这一页保持最简单直排 |
| `bfd7b95` | 默认主题定为**经典**（用户："经典"）；删掉示例配置里两个没人读的键（`ui_accent` / `ui_shell`）；配置测试增加"孤儿键"检查 | 示例配置与代码回退值原本打架（silicon vs classic） |
| `32ef6e6` | 语音识别修复：在所有已知缓存根里找模型并把 `HF_HUB_CACHE` 指过去；缺模型时**不再**锁离线；失败时给出可执行的三种修法；新增 `download_stt.py` | 用户实测日志里「开关开着却用不了」，详见第 3 节 |
| `a319f2e` | QQ 启动不再假报「1 次回复未送达」：`_safe_send` 增加 `count_fail`（控制类调用不计），`_health_tick` 未连上不发探针 | 日志里每次启动都出现的假警报（其实一条没丢） |
| `f382499` | 离线补拉的结论说清楚：新增 `offline_report()` + 拉取统计，分开"真没消息 / 没有可查会话 / 接口全失败" | 原来一句「无离线消息（或 get_friend_msg_history 不可用）」两种含义混在一起 |
| `be9d7fd` | 亚克力模糊默认开启（示例配置 `ui_acrylic: true`） | 用户答复："默认的要一点模糊，用户可自己改" |
| `714818a` | STT 模型挪进项目内 `models/stt`（HF_HOME=models/stt，权重在 `models/stt/hub/`）；`download_stt.py` 镜像模式关闭 Xet（修 401）；老目录 `models/hf` 仍被认 | 用户答复："可以移动，文件夹名称写清楚就行" |
| `4386786` | 总览页写明要启动哪一只：胶囊与启动按钮带活动角色显示名；「设为活动」提示用显示名并说清下一步去哪启动 | 用户问"怎么启动诺瓦？像写死了丛雨" —— 引擎其实认 `get_active_pet_id()`，是**看不出来** |

### 2.5 QQ / 微信 / NapCat

| 提交 | 改了什么 | 为什么 |
| --- | --- | --- |
| `bc8fe51` | 修回活泼模式不发言 + 私聊字数上限被长句绕过（都是协作者引入） | 功能回归 |
| `a7eda35` | 修 QQ 桥的 `TypeError`：同名 `_ws_auth_headers` 把静态方法盖掉 → 桥接**根本连不上** | 用户报"QQ 不理我" |
| `3846ea2` | NapCat WebUI 按钮打不开；NapCat / QQ 版本钉死不再跟随上游 | 上游新版会破 |
| `9622a83` | NapCat `4.18.14 → 4.18.28`，QQ 支持到 `9.9.35-52892`；只搬差异文件，保留 `config/ cache/ logs/ guild1.db` | 用户机器的 QQ 已经自动更新到 9.9.35，老版支持表够不到 |

### 2.6 打包 / 隐私

| 提交 | 改了什么 | 为什么 |
| --- | --- | --- |
| `7518b07` | 安装包不再可能打包进个人数据（微信凭据 / 聊天历史 / 主人照片） | 隐私红线 |
| `648e42b` | 打包器隐私自检不再误报第三方库 + 载荷去重 | 误报导致没法打包 |
| `f061771` | 一键安装器（协作者那套）质量审计后的 8 处修复 | zip-slip / 半装 / 写死 D: / 无卸载 / 跨线程 / 版本号 / 转义 |
| `ef17d05` | 重命名两个管理员脚本 + 长语音重启脚本（同步全部引用） | 名字与实际用途不符 |

## 3. 验证证据

- **自测 40 个全绿**，总耗时约 56 秒：
  ```powershell
  python run_all_tests.py        # 审计目录里，输出见 _logs/<脚本>.log
  ```
- **界面截图**：`shots_pages/` 下 7 个页面 + 设置页分类视图 + 对话框；本次重点看
  `07_settings_ai.png`（对话模型 = 本地 Ollama / DeepSeek / **Qwen**，
  推理等级 = **关** / 低 / 高 / 最高 —— 当前值一眼可见）、
  `04_prompt.png`（提示词页：两个卡片 + 状态行 + 固定底部按钮）、
  `dialog_info.png` / `dialog_confirm_danger.png`（新的主题对话框，危险确认键是红的）、
  `noir_touch_boxes.png`（诺瓦头 / 脸 / 胸三个框的位置）。
- **未验证（必须由用户实测）**：
  1. Live2D 模式下诺瓦那三个触摸框的位置（数值是按 2D 立绘量的，需要肉眼看）；
  2. `dist/` 与根目录的旧 exe 都是过期产物 —— 按用户要求**先不动**，也不假装修好了。

### 3.1 NapCat 4.18.28 实测：✅ 通过（用户 2026-09-24 日志）

用户提供了两段真实运行日志，结论：
- 扫码登录成功 → `NativePacketClient Hook 初始化成功 platform=win32.x64`
  （**没有**出现旧版本那句「PacketBackend 不支持当前QQ版本架构」）
- OneBot11 WS `127.0.0.1:3001` 已启动，`[QQBridge] ✅ WebSocket 已连接`
- 私聊收到「测试」→ 回了 7 条文字 + 1 张图 + 1 条语音 → **收发链路完全通**
- 启动横幅里能看到新的版本自检输出（`[版本] ✓ NapCat 4.18.28 与 NAPCAT_VERSION.txt 一致`）

顺带从日志里发现并修掉的两个老问题：
- **语音识别（STT）其实一直没工作**（`32ef6e6`）：`[STT] ⚠ 模型预热失败: Cannot find an
  appropriate cached snapshot folder ... outgoing traffic has been disabled`。根因是
  "找模型的目录"与"模型实际所在目录"不一致 + 无条件锁离线。修完在本机实测
  `warmup() → True`（16.2 秒加载）。
- **每次启动的假丢消息警报**（`a319f2e`）：`⚠ 活性探测 发送失败：连接尚未建立（累计 1 次发送失败）`
  + `🔗 已重新连接（此前断线期间 1 次回复未送达，请对方重发）`。其实是启动瞬间的探针
  在 WS 建好之前发出；一条回复都没丢。现在控制类调用不计入"未送达"，且未连上不发探针。
- 日志里 `⚠ GPU 初始化失败，使用 CPU` 属正常回退（该机 CUDA 不可用），不算问题。

## 4. 没做 / 推迟（含用户当日拍板的决定）

| 事项 | 决定 |
| --- | --- |
| 设置页 5 个枚举项改多选 | ✅ 本次已完成（`fa4a230`） |
| `_translucent()` 只认 0–1 alpha 的守卫 | ✅ 本次已完成（`fa4a230`） |
| `live2d_enabled` 默认值自相矛盾 | ✅ 本次已完成（`fa4a230`） |
| 交接文档 + 规范 | ✅ 本次建立（本目录） |
| 提示词页视觉整理 | ❌ 用户否掉（"不好看，用原来的"）→ 已回退（`81bebf3`），这一页保持直排 |
| 记忆页 / 插件页视觉整理 | 等用户发话（提示词页被否后不再主动动） |
| 60+ 处原生 `QMessageBox` | 只换「用户看得见」的那批；**已换 56 处**（`ad35a9f` + `120ba6e`），只剩 1 处崩溃类刻意保留（`pet_wizard` 创建失败带 traceback） |
| 8 处 `QInputDialog` 原生输入框 | ✅ 已完成（`f247e70`）：`InputDialog` / `ask_text`，契约与 `QInputDialog.getText` 完全一致 |
| 配置默认值不一致（11 处） | ✅ 全部定案：6 处对齐示例配置（`d000853`）、`ui_theme=经典`（`bfd7b95`）、`ui_acrylic=true`（`be9d7fd`）；只剩 `qq_stt_enabled` 一处刻意例外（随包安装器写 false） |
| `run_qq.py` 启动时的 QQ 版本自检警告 | ✅ 已完成（`6f0d5fd`）：逻辑抽到 `tool/napcat_version.py`，打包与启动共用 |
| 语音识别模型放哪 | ✅ 用户答复"可以移动，文件夹名称写清楚就行" → 挪进 `models/stt`（`714818a`） |
| `config.json` 里 3 个历史键（`longtext_ref_*` / `longtext_tts_type`） | 用户问"这三个是干嘛的" → 它们是**老版本的长语音参考音频/文本全局键**，现在改成按角色写在 `pets/<角色>/pet.json` 的 `voices.long_ref_audio` / `long_ref_text`，全局键**没有任何代码读**（README 已注明"已删除"）。用户的 config.json 我没动。 |
| arona / hiyori 的触摸区域 | **不做** |
| 删 `qq_bridge.self.napcat_token` | 谨慎处理：先确认全仓库没有任何地方在读写它再删 |
| `dist/` 与根目录旧 exe | 先不动 |
| 打包 / 一键安装包 | 按用户要求不做（`runtime/venv` 不存在，从未构建验证过） |

### 4.1 用户新交给的三件事（做这些时要记住的约束）

| 事项 | 用户原话 / 约束 |
| --- | --- |
| 立绘工坊的 2D 展示 | "2D 的展示还可以，审查一下有没有 bug，或者看看能不能优化就行了" → **只审查 + 优化，不要改设计** |
| 立绘工坊的 Live2D 展示 | "记得之前写过一个极简风的 live2d 调试的东西…用那个，再修修 bug 啥的，然后包装一下 UI" → 用那个极简调试器（`pcl_launcher/live2d_preview.py` + 根目录 `debug_live2d.py`），修 bug 再包装 UI |
| 启动别的桌宠 | "如何启动诺瓦或者其他桌宠？…似乎写死丛雨" → ✅ 已查清是"看不出来"而非写死，并已补字（`4386786`） |

## 5. 下一步建议

1. **立绘工坊 2D 展示审查**（用户新交办）：只查 bug + 看能不能优化，**不改设计**。
   入口 `pcl_launcher/portrait_studio.py`；`DisplayPreview`（2D 预览）与 Live2D 预览分开审。
2. **Live2D 调试器包装成 UI**（用户新交办）：那个"极简风、用来看每个动作/表情打标签"的东西
   是 `pcl_launcher/live2d_preview.py`（`Live2DPreviewWidget` / `Live2DPreviewWindow` /
   `open_live2d_window`）与根目录 `debug_live2d.py`。做法：修 bug → 包一层主题一致的 UI →
   接进立绘工坊的 Live2D 页。**注意用户要求"包装一下 UI"，先问清要不要动立绘工坊的布局**。
3. **记忆页 / 插件页视觉整理**：提示词页被否后不再主动动，等用户发话。
4. 全仓扫描器复查结果（2026-09-24）：孤儿 import 0 / 未定义名 0 / 不可达代码 0 /
   模块级先用后定义 0 / 持锁重入 0 / 同名重复定义 0 —— **没有可清理项**。
5. 可选清理：`qq_bridge.self.napcat_token`（已确认只写不读）。

## 6. 风险与回滚

- **NapCat 升级**：回滚用升级前的整份备份目录，或只还原被覆盖文件的那份局部备份
  （两份都在项目父目录下，名字里带 `4.18.14`）。
- **设置页控件类型变化**：`git revert fa4a230` 即可。取值 / 落盘接口没变
  （`BoolSwitch` 与 `ChoiceRow` 都实现 `QSlider` 的子集），**配置文件格式零变化**。
- **透明化改动**：只影响「背景色写法」的识别，`revert fa4a230` 恢复旧行为。
- **触摸区域**：只改了 `pets/noir/pet.json` 一个角色包，删掉 `touch` 段即回到"无触摸"。
- **提示词页改版**：`git revert b59c12f`。保存链路 / 文件名 / 落盘位置都没变，
  只是排版与状态显示；人设文件格式零变化。
- **弹窗迁移**：`git revert ad35a9f`。所有对话框都有系统弹窗兜底
  （`message()` / `confirm()` / `ask_text()` 里 try/except），迁移不会让任何流程卡死或静默失败。
- **配置默认值对齐**：`git revert d000853`。改的都是"键缺失时"的取值 ——
  已有 config.json 的用户（键都在）行为完全不变。
- **STT 模型位置**：`models/stt`（约 2.9 GB，`models/` 已在 `.gitignore` 里，不会进仓库）。
  换目录只影响 `tool/stt.py` 的 `HF_HOME` 与 `download_stt.py`；老目录 `models/hf`
  仍在候选根里，所以不会因为"换了名字"而认不出已下好的模型。
  ⚠ 记一笔失误：挪完模型后我用一条多余的 `Remove-Item` 把目标目录删了、用户缓存里的源
  也已被删 → 重新从镜像下载（7 分钟）才恢复。**动模型/缓存这类大文件前先确认没有清理命令混在同一行。**

## 7. 需要用户确认的问题

1. ~~NapCat 4.18.28 实测~~ → ✅ 用户已实测通过（见 3.1）。
2. 诺瓦的 Live2D 触摸框位置是否合适（2D 已按立绘量过）？
3. ~~记忆 / 插件页是否继续整理~~ → 提示词页被否，用户没再提；**不再主动动排版**。
4. ~~`ui_theme` / `ui_acrylic`~~ → 已定：经典 + 亚克力默认开（`bfd7b95` / `be9d7fd`）。
5. ~~3 个历史键要不要清~~ → 用户问"是干嘛的" → 见第 4 节说明；**用户数据没动**。
6. ~~STT 模型挪不挪~~ → 已挪进 `models/stt`（`714818a`）。
7. **新**：立绘工坊 Live2D 那块的"包装 UI"要不要动立绘工坊现有布局？（用户说"包装一下 UI"，
   我倾向：只把调试器包成主题一致的独立窗口，再从立绘工坊按钮打开，**不动现有排版**）

## 8. 给接手人的三个提醒

1. **审计目录不随仓库走**：全部自测 / 探针 / 渲染脚本都在仓库外的临时目录里。
   换机器后只有仓库里的代码 —— 那时需要先重建测试，别以为"没测试就是没问题"。
2. **用户数据一律先快照**：渲染、探针都可能触发写盘
   （曾经把 `pets/murasame/pet.json` 的显示模式改掉）。渲染脚本里已有快照 / 还原。
3. **不推送、不碰远程**：这是用户这几天的硬要求，直到用户明确说"推送"为止。

## 9. 追加（同日晚些时候）：Live2D 动作时间轴 + 四个角色的 Live2D 情绪映射诊断

### 9.1 清掉 `config.json` 里 3 个历史键（用户："清掉"）

`longtext_ref_text` / `longtext_ref_voice` / `longtext_tts_type` 是**老版本的长语音全局键**，
现在按角色写在 `pets/<角色>/pet.json` 的 `voices.long_ref_audio` / `long_ref_text`，全局键**没有任何代码读**。
已从用户 `config.json` 删除（52 → 49 键，其余键值逐项比对完全一致）：

```
backup_config_20260924-135111.json     # 备份（在审计目录里，不在仓库）
python clean_legacy_config_keys.py --apply   # 审计目录里的清理脚本
```

⚠ `config.json` 是 gitignore 的用户数据，所以这次清理**没有仓库提交**，只有文档记录。

### 9.2 Live2D 调试器加「动作时间轴」（`cb7da41`）

用户纠正了我之前的理解：他要的不是"设置页里那种简单调试"，而是

> 可以跟着鼠标拖动一个进度条，一个动作开始改变

（因为现在"无论谁、几个人似乎都是一直点头或者固定晃动/动作"，动作只会从头播到尾，中间过程看不清。）

做法（`pcl_launcher/live2d_debugger.py`）：**先录再定格**。
Live2D 的 `Update()` 不带 dt，动作靠真实时间推进，没法直接"跳到第 N 帧"，
所以先边播边逐帧快照 72 个参数；拖进度条 = 把那一帧的参数 `SetParameterValue` 写回去，
同时 `StopAllMotions()` + **不再 `Update()`**（只 `Draw()`）→ 画面定住。

| 细节 | 处理 |
| --- | --- |
| 定格期间自动眨眼/呼吸 | 关掉（否则会跟定格抢参数），解除定格时恢复 |
| 换表情 / 点动作时 | 自动解除定格（否则画面不更新，看着像点了没反应） |
| 「重置参数」 | 先解除定格再重置 |
| 录完的反馈 | 列出这段动作**真正在动的参数**（名字 + 变化范围），顺手当"这个动作在动什么"的说明书 |
| 录制上限 | 900 帧（约 15 秒）防呆，动作播完即停 |

顺手修掉的三个界面问题（都在同一个窗口）：

1. **画布背景是白的**：Qt 画 FBO 时会把清屏色改回白色 → 必须每帧在 `paintGL` 里重设
   （原来只在 `initializeGL` 设了一次）。
2. **右栏被切**：滚动区只有 274px 而面板写死 400px，且关掉了横向滚动条 → 表情/动作
   第二列整列看不见。改成"面板宽度跟着滚动区走 + 滚动区固定 420px"。
3. **长文件名顶破面板**：按钮只显示短名（`motion01`），完整文件名放 tooltip 与对照表；
   长标题允许换行；"保存/复制/导出"三个按钮改两列排。

证据（真实 GL，走界面代码路径而不是直接戳 live2d API）：

```
probe_dlg_timeline.py    丛雨 motion01 → 录到 280 帧、47 个参数在动；
                         拖到第 47 / 234 帧定格，参数与录制帧 72/72 一致；
                         两帧画面 4.7% 采样点不同（grabFramebuffer，grab() 抓不到 GL）
shots_pages/timeline_f47.png / timeline_f234.png   整窗截图（画布是 GL，需把帧缓冲贴回去）
test_live2d_debugger.py  [8] 段以子进程跑上面的探针（offscreen 平台起不了 GL）
```

### 9.3 Live2D"换不了表情"的真因：【白】标签的括号是全角，正则只认半角（已修）

用户反馈："live2D 模式下还是不能做到像丛雨 2D 一样换表情，衣服（对应为动作）"。
用户当时的拍板（原话复述）：**方案 A：句内标签**——人设里写「情感特别特别激烈时在句首加【白】」，
`pet.json` 里加 `"白": ["white.exp3.json"]`，标签**只覆盖"显示用情绪"（表情/动作）**，
语音照旧按原始情绪合成（选音色那套不变），回复结束回默认。

代码里确实就是按这个实现的（`classes/Worker_class.py` 两个 Worker 各一处，注释里写的例子就是【白】）：

```python
# 句内【情绪】标签（如【白】）→ 只覆盖「显示用情绪」（表情/动作），
# TTS 已按原始情绪合成，语音不受影响。
for i, t in enumerate(reply_raw):          # 从**清洗前**的原始句提取
    m = _re.findall(r'\[(.+?)\]', str(t))  # ❌ 只匹配半角 [白]
```

而人设里写的是**全角**【白】（`pets/noir/prompt.txt`、`longtext_prompt.txt`）。
`git log -S` 查到这段从 **v1.12.1（`4214f5d`）** 起就是这样 —— 也就是说
**诺瓦的"白形态"这条唯一由人设驱动的表情路径从来没触发过**（除非模型自己吐半角 `[白]`）。
不是缺文件：四个角色引用的表情/动作文件**零缺失**，`white.exp3.json` 也在。

修法（最小改动，不新增分支、不动数据）：

| 文件 | 改动 |
| --- | --- |
| `classes/Worker_class.py` | 新增模块级 `EMOTION_TAG_RE = re.compile(r'[\[【]([^\[\]【】]{1,6})[\]】]')` + `extract_emotion_tag()`；两处覆盖逻辑改为调用它（限定 1–6 字、不跨括号，避免把【一大段动作描写】当情绪词） |
| `classes/murasame_class.py` | 两处同样的半角正则（Live2D 句起取标签 / 长文本流式取标签）改用同一个函数 —— 四处各抄一份正是这个 bug 的成因 |

验证：

```
test_emotion_tag.py   全角【白】/半角[白]都取得到；【动作描写】不当情绪词；
                      显示用情绪被覆盖而**语音情绪列表不变**；标签不漏进显示/朗读文字；
                      四个角色引用文件零缺失；诺瓦「白」→ white.exp3.json 解析正确
probe_noir_white.py   真实 GL：white.exp3.json 只驱动 Param41 →
                      默认 Param41=0.000 → set_emotion("白") → 1.000（画面 14.9% 采样点不同，
                      截图 shots_pages/noir_white.png 白发光头明显）→ 收尾 set_emotion("") → 0.000 回黑发
```

> ⚠ 自我纠错（教训）：修好之前我按"运行期那 6 个语音情绪词会驱动 Live2D 表情"这个**我自己推断的**
> 假设，算了一张"覆盖率表"并准备去改四个角色包的映射 —— 用户当场制止（"这些不都是咱们之前调好的？"）。
> 真实设计是"语音情绪管音色、句内标签管显示"，代码注释里就写着，**应该先读注释/人设再下结论**。
> 那张表（`check_live2d_emotion_coverage.py` 的输出）只能当"哪些词没有对应素材"的信息，
> **不是待办清单**；`default_expression` 留空也是对的（空 = 素脸常态 = 收尾要回到的那个样子）。

### 9.4 这一段的可复现命令

```powershell
python E:\university\order\python\pet\_audit_fish9269\run_all_tests.py            # 全部自测
python E:\university\order\python\pet\_audit_fish9269\test_emotion_tag.py          # 句内标签（全角/半角）
python E:\university\order\python\pet\_audit_fish9269\probe_noir_white.py <仓库根>  # 诺瓦白形态（真实 GL，看 Param41）
python E:\university\order\python\pet\_audit_fish9269\probe_dlg_timeline.py <仓库根>   # 调试器时间轴（真实 GL）
python E:\university\order\python\pet\_audit_fish9269\check_live2d_emotion_coverage.py <仓库根>
#   → live2d_emotion_coverage.txt（UTF-8，控制台是 GBK 会花，别用管道看中文）
```

## 10. 再追加：半身触摸比例、Live2D 右键、换角色即时切换（用户当晚三条交办）

### 10.1 诺瓦是半身 → Live2D 触摸比例重配（`f0f9755`）

用户："诺瓦是半身，所以触摸比例要重新搞"。

先按桌宠**真实显示参数**（`live2d_window_ratio=1.0`、`live2d_scale=1.53`）渲一张 850×850
带 0.05 网格的图（`probe_noir_touch_grid.py`），照图量出半身的实际位置：

| 部位 | 旧值（照全身/2D 量的） | 新值（半身实测） | 说明 |
| --- | --- | --- | --- |
| head（猫耳+头顶） | `[0.32, 0.02, 0.38, 0.18]` | `[0.12, 0.18, 0.74, 0.28]` | 旧框整个悬在模型**上方**（模型头顶从 y≈0.19 才开始） |
| custom_1（脸） | `[0.38, 0.28, 0.28, 0.20]` | `[0.30, 0.46, 0.35, 0.31]` | 旧框落在头发/猫耳上 |
| chest（领口/裙子） | `[0.34, 0.55, 0.34, 0.18]` | `[0.26, 0.77, 0.48, 0.22]` | 旧框正好压在**脸上** |
| 摸头区间 `head_top/bottom` | `0.0 / 0.22` | `0.18 / 0.46` | 与 head 框对齐（旧值 = 摸头区悬空） |

只动了 `areas_live2d` 与 `interaction.head_top/head_bottom`；`areas_2d` / 旧的 `areas` / 禁用表
一律没碰（诺瓦是纯 Live2D 角色，没有 2D 素材）。证据图：
`shots_pages/noir_touch_boxes.png`（框画在模型上，正好框住猫耳/脸/裙领）。

> ⚠ 量的时候踩过一个坑：`hit_area()` 取**面积最小**的那个命中，而且它自己**不管禁用表**；
> 禁用是桌宠在命中前过滤的。所以量之前必须照桌宠的做法先滤掉 `disabled_live2d` 里那 12 个
> 默认部位，否则会被"小腹/四肢"的默认小框抢走命中。

### 10.2 Live2D 模式下补上右键菜单（`f0f9755`）

用户："live2D模式下也要有右键的功能"。

原因很直接：Live2D 模式下桌宠窗口是 `hide()` 的，右键事件只有 `Live2DWidget` 收得到，
而它的 `mousePressEvent` 只处理了左键（触摸）和中键（拖动）→ 换装、输入对话都点不出来。

- `Live2d/live2d_ui.py`：新增 `context_menu` 信号，右键发全局坐标
- `main.py`：接到 `pet._show_outfit_menu`（与 2D **同一个菜单**，不新增一套）

顺手修掉一个交互冲突：以前一次左键既触发触摸反应、又可能命中"点下半身 → 输入框"
（半身模型戳胸口时最明显）。现在桌宠判定命中触摸区域后，会在控件上留一个记号
（`_last_touch_hit`），控件那一次就不再走摸头/输入逻辑；**没命中时行为完全不变**。

### 10.3 顺手修：`pet._pet_cfg` 从来不存在（`f0f9755`）

用户日志里那句 `[Live2D] ⚠ 读取独立显示设置失败: 'Murasame' object has no attribute '_pet_cfg'`
是真 bug：`Murasame.__init__` 里只有同名**局部变量**，而 `main.py` 读 `pet._pet_cfg` 拿
角色单独调的 Live2D `scale/offset/窗口比例` → 每次都失败并被静默忽略。
现在存成实例属性，并把那段抽成 `apply_live2d_display_override()`（可测、取不到时安静跳过）。

### 10.4 「设为活动」直接换掉正在跑的桌宠（`edb5a2c`）

用户："每次设置活动，可以立即切换桌宠，0.5秒，而不是等6秒" → "0.5秒防误触就行了"。

以前：只写注册表 + 弹一句"桌宠正在运行：关掉再启动就会换成它"，得用户自己关了再开；
启动之后还要**固定等 6 秒**才刷新状态。

现在：`0.5 秒单次定时防误触`（连点几个角色只切最后那个）→ 点下去立刻给反馈（芯片+状态行）
→ 定时到点后：`shutdown` 关掉旧桌宠 → 用**新活动角色**重新 `run.py` → 0.5 秒轮询等它回来
→ 通知界面收尾（按钮恢复 + 刷新状态）。启动桌宠的固定 6 秒等待也换成 0.5 秒轮询。

⚠ **这一版是"关掉再用新角色起来"**：真正看到的切换要等桌宠启动（Live2D 冷启动几秒）。
用户要的"0.5 秒就换好"需要**热切换**（不重启进程）：热重读角色配置 + 热换 Live2D 模型 +
换记忆仓/触摸表。改动量与风险都大（`Murasame.__init__` 里 130 行角色配置读取要抽出来，
Live2D 控件要能销毁重建），**已在问用户要不要做**。

### 10.5 这一段的命令

```powershell
python E:\university\order\python\pet\_audit_fish9269\test_live2d_interaction.py   # 半身触摸 + 右键 + 触摸优先
python E:\university\order\python\pet\_audit_fish9269\test_pet_switch.py           # 换角色（0.5 秒防误触）
python E:\university\order\python\pet\_audit_fish9269\probe_noir_touch_grid.py <仓库根> 850
#   → shots_pages/noir_touch_grid.png（量尺寸用）、noir_touch_boxes.png（框叠在模型上）
```

## 11. 再追加：模型名换到 `deepseek-flash`（对话 + 看图同一个）

用户："Deepseek模型名字变了，及时更新，DeepseekFlash。在模型选择列表之中。对话和看图都要换哦，
已经没有 Deepseekv4Flash 和 vision-exp 模型了。"

DeepSeek 这一轮的改名（代码注释里也写清了）：

| 模型名 | 状态 |
| --- | --- |
| `deepseek-chat` / `deepseek-reasoner` | 2026-07-24 停用 |
| `deepseek-v4-flash` / `deepseek-v4-pro` / `deepseek-v4-flash-vision-exp` | 其后下线 |
| **`deepseek-flash`** | **现役唯一；对话与看图是同一个模型（不再有单独的 vision-exp）** |

改动（`46da719`）：

| 位置 | 改动 |
| --- | --- |
| `longtext/model_config.py` | 新增常量 `MODEL_DEEPSEEK_FLASH = "deepseek-flash"`（以后改版只改这一处）；`FAMILY_DEFAULT_MODEL["deepseek"]` 与 `DEFAULT_VISION_MODEL` 都指向它；文件头注释更新 |
| `pcl_launcher/widgets.py` | 三处模型候选列表（短文本 / 长文本 / 视觉）换成 `deepseek-flash`、删掉旧名；长文本与视觉的默认值 + 设置页载入回退值一起改 |
| `config.example.json` | `longtext_model_name` / `vision_model_name` → `deepseek-flash` |
| `README.md` | 模型表 / 配置键表 / 模型可配置化说明 / 迁移提醒 四处 |
| 用户 `config.json` | `longtext_model_name`: v4-flash → flash；`vision_model_name`: qwen3-vl-plus → flash（**只动这两键**，备份在审计目录；`short_model_name` 仍是用户选的 `qwen-plus`，没动） |

⚠ URL 路由是按**模型名前缀**判厂商的（`deepseek*` → api.deepseek.com，其余 → dashscope），
所以换名字不用改路由；但**视觉现在花的是 DeepSeek 那把 Key**（以前是 Qwen 的）——
实测用户两把 Key 都在，`get_vision_model_config()` 正常返回 deepseek-flash + DeepSeek 网关。

验证：`test_model_names.py`（新增）——三处列表含新名且无旧名、族/视觉默认、三条链路解析
（用临时 config，不碰用户数据）、URL 路由、示例配置与面板默认一致、README 不把旧名当可选。
全量 54 个自测全绿。

> 另记：`pcl_launcher/themes/silicon/theme.json` 的名字从「Silicon 现代（推荐）」变成了
> 「Silicon 现代」（16:04 改的，git 里还没提交）。不是这轮改的（可能是主题页里重命名过），
> **没动它**，等用户确认要提交还是还原。

## 12. 用户对"还没做"的四条拍板（`c3c09cb` / `df54f01`）

| # | 事项 | 用户答复 | 结果 |
| --- | --- | --- | --- |
| 1 | 真·原地热切换角色 | **现状** | 不做；保持"0.5 秒防误触 → 自动关旧开新 → 0.5 秒轮询刷新" |
| 2 | Live2D 给列表让 AI 自己选表情/动作 | **改** | ✅ 本次实现（见下） |
| 3 | 记忆页 / 插件页视觉整理 | **现状** | 不动 |
| 4 | 「当前使用」空槽显示 + 徽章色 | 桌宠槽默认丛雨、另两个默认空槽；徽章统一跟随主题 | ✅ 本次实现 |

### 12.1 Live2D 由 AI 自己选表情/动作（`df54f01`）

2D 立绘一直是"把图层列表交给 AI 让它选"（`portrait_prompts.json` 的 `layers_desc` + `example`）；
Live2D 这条链路以前只有两个来源：句内 `【情绪】` 标签 + 语音情绪列表 —— **没有**"给 AI 列表让它选"。
现在补齐：

| 位置 | 改动 |
| --- | --- |
| `pets/pet_registry.py` | `get_live2d_choice_words()`（= `model.emotions` 键 + `model.motions` 键，表情优先去重）；`get_live2d_prompts()`（角色可选 `live2d_prompts.json`：`prompt_template` / `extra_words`） |
| `tool/chat.py` | `build_live2d_prompt()`（两个模型族共用）：把该角色词表交给 AI，要求"分句数=词数、逐句一个、不许自编词、返回纯 JSON 列表"；`ollama_qwen3_portrait(..., live2d=True)` 走它（请求名 `ollama-qwen3-live2d`） |
| `tool/cloud_API_chat.py` | `cloud_portrait(..., live2d=True)` 同款（请求名 `*-live2d`） |
| `classes/Worker_class.py` | 两个 worker 都加 `live2d` 参数并透传 |
| `classes/murasame_class.py` | 发请求时按**当时**的模式传标志（并记 `_reply_live2d`）；新增 `_parse_ai_emotions()` / `_resolve_display_emotion()` |

**显示情绪优先级**：`句内标签（如【白】）` > **`AI 选的表情`** > `语音情绪列表（兜底）`。
⚠ **语音那套没被覆盖** —— `emotion_list` 依旧只喂 TTS 选音色（用户当时的要求）。

为什么"只在 Live2D 那轮"才把立绘返回值当表情词：2D 模式下同一个返回值是**图层 ID**
（`["1715","face_smile"]`），硬当表情词会乱套 —— 所以按**发请求时**的模式判定，存在
`_reply_live2d` 里，而不是解析时现读 `self._live2d_mode`（回复回来时模式可能已被切走）。

### 12.2 槽位显示与徽章色（`df54f01`）

- **桌宠槽**：总是显示当前生效角色（没显式指定就是默认角色「丛雨」）—— 桌宠永远得有个形象，
  这个槽不给虚线空态；从槽里拖出去 = 取消指定 → 回到默认角色（提示里写明是谁）
- **QQ / 微信槽**：显示**你显式指定的**；没指定就是灰色虚线框 + 加号，提示写
  "空 = 跟桌宠一样（<当前生效角色名>）"
- 卡片上的槽位徽章：三个槽共用一个颜色，且改用**主题强调色**（`accent_hex()`），不再固定用绿

用户数据：按"另外两个默认空槽"，清掉了 `config.json` 里显式的 `qq_active_pet` / `wechat_active_pet`
（行为不变，空 = 跟桌宠一样 = 丛雨；备份 `backup_config_20260924-164311.json`）。

### 12.3 顺手收掉的两件旧账（`c3c09cb`）

- `Live2d/live2d_ui.py` 的 `_apply_exp`：配了表情却找不到文件时**不再静默 return**，
  打印一次可见警告（日和就是靠内置表指到丛雨的 `exp1/exp6`，一直静默失败）
- `qq/qq_bridge.py`：删掉只写不读的 `self.napcat_token`（交接 §4 挂着的清理项，已确认无人读）

### 12.4 顺带验证："其他地方看不清字"还有没有

下拉（QComboBox 弹出窗）是三处弹出类里唯一真坏的（已修）；**QMenu 实测没问题**
（右键菜单四个主题对比度 8.66 / 9.18 / 15.00 / 9.18，`probe_menu_contrast.py`），
QToolTip 本来就有显式颜色，列表控件走内联样式 —— 不需要再改。

### 12.5 这一段的新探针/自测

```
test_live2d_ai_choice.py   提示词按角色生成 / 模板覆盖 / 解析与优先级 / 标志透传 / 云端走 Live2D 提示词
test_pet_slots_ui.py       跟着"桌宠槽总能填着、另两个空槽"更新
probe_menu_contrast.py     弹出菜单对比度（四个主题）
```


## 13. 再追加：对话框"看不清字"整族修复 + Live2D 画布崩坏/屏闪的真正根因（`d830e94` / 见文末哈希）

用户当晚报四条，前三条都是同一族问题：**老界面是深色底 + 写死的浅色文字**，
而主题系统支持浅色主题（经典 / 千恋万花）后，浅字压浅底就看不清了。

### 13.1 用户报的四个点（截图）与实测

| 用户说法 | 实测（修复前 → 修复后） |
| --- | --- |
| 图二右上角「立绘工坊」四个字对比度低 | `#e8e8f0` 压 `#eaf2fe` = **1.08:1** → 用主题 `Color1` = **9.74:1**（樱华 10.71 / Silicon 14.42） |
| 图一左下角的字对比度低 | Live2D 预览窗口底部按钮：画布写死深色 `QColor(30,30,38)`，按钮文字却跟主题走（经典/樱华是**深**字）→ 深底深字，只剩 emoji 看得见 → 该窗口恒为深色，**把底和字一起钉死** = 12.35:1 |
| 「立绘工坊以及其连带的东西」 | 触摸区域调节标题 1.08 → 9.74；两处提示 2.46 → 5.58；部位说明 2.47 → 5.59；角色向导 11 处 `#888` 3.14 → 5.58 |
| （同类，顺手） | 状态字 `#8fd18f` / `#7fc48f`、警告字 `#e07a90` → 新增 `ok_text()` / `warn_text()`；全局 QSS 的 `QPushButton:disabled` 写死 `rgba(255,255,255,0.35)`（浅色主题下禁用按钮 = 看不见）→ 改主题 `Gray3` |

量法（**不是读样式表猜颜色**，是量真实渲染像素）：`probe_dialog_contrast.py`
逐 (3 主题 × 5 对话框) 起真实窗口（`WA_DontShowOnScreen`，字体引擎正常），
整窗 `grab()` 后把每个带字控件的地图坐标裁出来，底=出现最多的颜色、字=**反复出现且离底色最远**的颜色，
算 WCAG 对比度，低于 4.5:1 标 `!!`，报告落 `dialog_contrast.txt`。
两个坑记一下：① 不能用"与底色差最大的单个像素"当字色 —— emoji（🔄🎭🖼）是彩色字形，
会把深底深字量成 12:1（预览窗口就是这么被量错的，所以按钮另加"跳过左侧图标区"）；
② 被滚动区裁掉的控件要按 `visibleRegion().isEmpty()` 跳过，否则地图坐标落到别的部件上量出假的 1.00:1。

`pcl_launcher/colors.py` 新增 `rel_luminance / contrast_ratio / readable_on / ok_text / warn_text`：
不写死替换色，**按当前主题底板算对比度**（浅底压暗、深底提亮），用户自选「启动器底色」
(`ui_bg_color`) 时也跟着对；本来就够清楚的颜色原样返回，不动设计色。

### 13.2 Live2D 画布"崩坏 / 频闪"的真正根因（`d57ca2d` 只是绕开，没治本）

用户复现：工坊→预览窗口（正常）→关掉→调试器 = **频闪**；重启桌宠后 工坊→调试器（黑画布）→预览窗口 = **崩坏**。
关键前提：**启动器一个进程里最多同时有三个 Live2D 画布** ——
① 立绘工坊里那块**内嵌**预览（角色没有 2D 素材时显示 Live2D，用户日志里 `[PCL] Live2D 模型加载成功` 就是它）
② 「打开实时预览窗口」的独立窗口 ③ 动作/表情调试器的 `ModelCanvas`。

根因：Cubism 原生引擎是**进程级全局单例**，`LAppModel` 的 GL 资源绑在**第一个**建立起来的 GL 上下文上；
后建的画布在自己的上下文里画不出东西（实测帧缓冲**模型墨迹 0.0%、帧间跳变 0.0%** = 完全空白/冻结）。
`probe_l2d_two_canvases.py` 三种画布同时开的实测：

```
SHARE=False（改前）: 画布1内嵌 墨迹 40.3% 动  |  画布2预览 0.0% 冻  |  画布3调试 0.0% 冻  |  画布2复测 0.0%
SHARE=True （改后）: 画布1内嵌 40.3% 动        |  画布2预览 39.8% 动 |  画布3调试 29.1% 动 |  画布2复测 40.6% 动
```

修法：入口在**创建 QApplication 之前**打开 Qt 的共享 GL 上下文开关
（`silicon_ui.enable_shared_gl_contexts()`，silicon_window.launch / debug_live2d.py 各调一次）。
窗口摆到 (-4000,-4000)：真实 GL 上下文（offscreen 平台起不了 GL）但不出现在屏幕上；
判据用 `grabFramebuffer()` 的"模型墨迹"（与画布清屏色差得多的像素占比）+ 帧间跳变，
**不能用"非黑像素"**（清屏色本身是深色，非黑占比恒 100%，量不出模型画没画出来）。

### 13.3 这一段的新探针 / 自测

```
probe_dialog_contrast.py    3 主题 × 5 对话框逐控件量真实像素对比度（报告 dialog_contrast.txt + 每格 PNG）
probe_combo_blue.py         查"下拉框文字发蓝"到底是谁上的色（结论：是 emoji 干扰，字其实是深色）
probe_l2d_two_canvases.py   preview/debugger/keep/destroy/reverse_keep/triple 六种画布组合，SHARE=0/1 对照
test_dialog_contrast.py     对比度工具 + 每主题 Color1/Gray2 ≥4.5:1 + 源码遗留色白名单 + 预览窗口按钮
test_theme_and_canvas.py    追加：多画布共享 GL 上下文（子进程真开 app 验 testAttribute + 入口调用顺序）
```

**遗留 / 待用户确认**：
1. ~~自动关闭~~ **已按用户拍板去掉**（见 §14.1）。
2. 「已保存品牌红」按钮（`#c8506e` + 白字）是 4.35:1：视觉清楚、属刻意设计，没动。
3. 插件页卡片用的 `rgba(255,255,255,150)` 大透明度白底在 **Silicon 深色主题**下会变成浅灰板
   （配 `Color1` 浅字可能偏淡）—— **已修，见 §14.2**。


## 14. 用户拍板后的收尾：去掉互斥关闭 + 插件页深色主题"卡片发白"

### 14.1 允许"实时预览窗口 + 调试器"同时开着

用户拍板去掉自动关闭（怕正在打标签的调试器被预览窗顶掉）。改法：
`open_debugger` 不再关预览窗口、`open_live2d_window` 不再调 `close_open_debuggers`
（该函数一并撤掉，不留死代码）；`test_theme_and_canvas.py` 的断言反过来：
**开一个不许关另一个** + 旧的关闭函数必须已撤销。

### 14.2 插件页卡片：`rgba(255,255,255,150)` 写死白膜 = 深色主题下浅灰板

实测（`probe_page_contrast.py`，真实窗口 + 逐控件真实像素对比度）：

```
改前  silicon/plugins 26 个低于 4.5:1（卡片 #9e9fa4 底 + 浅字 = 1.2:1，几乎看不见）
      classic/plugins  4 个（选中胶囊 #a6c9fb + 白字 1.70、导入/刷新 1.72、页脚 1.57）
改后  silicon/plugins  3 个（只剩"白字压主题亮蓝 #4c8dff = 3.20:1"的实心主按钮，刻意观感）
      classic/plugins  1 个（只剩页脚那条，见下）
```

三处根因、三种改法：

1. **写死的"磨砂白"** `rgba(255,255,255,150)` → 新增 `colors.surface_fill()`：
   浅色主题给乳白半透明（透出壁纸，观感不变），深色主题给极淡白膜（`rgba(255,255,255,22)`，
   和 `silicon_qss` 里 `0.03~0.06` 一个路子）。用在插件页的卡片与分类胶囊上。
2. **`_make_transparent` 把"实心强调色 + 白字"也压到 0.35** → 浅色页面上变成"淡蓝底 + 白字"
   （实测 1.70:1）。现在这种块**保持不透明**：判据 = 样式表里**任一** color 声明是浅色
   **且**任一背景是彩色（`saturation() > 60`）。⚠ 必须扫全部 `color` 声明 ——
   只看第一处会拿到未选中态的深色文字 → 漏判（这个坑我踩了一次）。
3. **`Gray3` 当页脚文字** → `Gray2`（浅色侧栏上 1.57 → 5.2:1）；「我的」标签写死的
   `#c98a1e` 在浅色卡片上只有 2.9:1 → `readable_on(QColor("#c98a1e"))`。

### 14.3 全页面体检：7 个页面 × 主题，逐轮修完的数字

`probe_page_contrast.py`（真实窗口 + 逐控件真实像素对比度；`!!` = 低于 4.5:1）：

| 页面 | classic 改前 → 改后 | silicon 改前 → 改后 | 剩下的是什么 |
| --- | --- | --- | --- |
| plugins | 4 → 1 | **26 → 3** | 白字压主题亮蓝 `#4c8dff` = 3.20:1 的实心主按钮（刻意观感） |
| settings | 13 → 11 | **11 → 3** | classic 侧：`Gray2` 压浅灰膜 3.2~4.4、`使用中`（禁用态）2.06、分段按钮 3.9~4.1 |
| pets | 17 → 14 | 17 → 13 | 徽章：`Live2D`(#d4a020) 白字 2.37、`短语音/触摸/长语音`(#30a030) 3.39、`设为桌宠角色`(#21aa11) 3.08；角色名（蓝底深字）3.5 / 4.2 |
| memory | 2 → 2 | 1 → 1 | `备份该角色记忆`（绿底白字）4.07 |
| themes | （新量）5 | （新量）2 | `旧版主题 当前使用` 3.24、`使用中`（禁用态）2.06、主按钮 3.20 |

这一轮改了（`widgets.py` / `themes_panel.py` / `silicon_dialog.py` / `pet_slots.py` / `silicon_window.py` / `plugins_panel.py`）：
- **8 处写死的大 alpha 白膜**（`rgba(255,255,255,120/150/170/175/190)`：设置页分类胶囊、
  分区标题、输入框、下拉框与它的弹出列表、密钥「显示」按钮、次要按钮、主题页分区标题、
  对话框次要按钮）→ 全换 `surface_fill(浅色原值, 深色膜)`。现在 `pcl_launcher/` 里
  只剩 `colors.surface_fill()` 自己那处；
- **`Gray3` 当说明文字 → `Gray2`**（设置页底部"改动会自动保存"、桌宠页空状态、主题页
  主题目录与「共 N 个」、槽位提示、插件版本号、侧栏页脚 —— 含**换肤时重新上色**那条路径，
  它原来会把页脚又刷回 Gray3）。

**还剩什么 / 为什么不动**（都记了实测数字，随时可继续）：
1. **彩色徽章 + 白字**（2.37~4.07）：`Live2D` 琥珀、`短语音/触摸/长语音` 绿、`设为桌宠角色`、
   `备份该角色记忆`。它们是**刻意的语义色徽章**，用户这次选的是"接着修页面"而不是
   "连徽章一起提对比度" → 保持原样。
2. **侧栏页脚** `Silicon UI · 新版`（2.50）：文字已经是主题次级色，低是因为**侧栏自己的
   半透明灰底**（老深色 UI 遗留）压出来中灰。要真解决得动侧栏底色，属观感改动，没动。
3. classic 侧 `Gray2` 压浅灰膜 3.2~4.4 的几处（设置分区标题、行标签）：差一点点，
   动它们要改布局底色，等用户看到实物再定。

⚠ 测量前提：这一轮跑的是**没有壁纸**的 classic / silicon（`_page_block_alpha()` = 0.35）。
用户实际用的 `senrenbanka` 系主题有壁纸 → 该函数给 0.92，同一处数字会不一样
（用户已表示不必按他的主题再量一遍）。

### 14.4 这一段的新探针 / 自测

```
page_contrast_inner.py / probe_page_contrast.py   启动器 5 个页面逐主题逐控件真实像素对比度（page_contrast.txt + 每页 PNG）
probe_plugins_chip.py                            单看分类胶囊被画成什么色（结论：选中态被压透明，不是 Color3 的问题）
test_alpha_guard.py                              追加：[3]「实心强调色 + 白字」原样 / [3b] 彩色底深字与中性膜照常压
test_dialog_contrast.py                          追加：surface_fill 浅/深两态 + 插件页不再写死白膜 + 页脚/透明化豁免
```


## 15. 收尾：版本 V1.17.2（README 条目 + 版本锚点）

### 15.1 「项目版本」的四处锚点（本次统一到 1.17.2）

「项目版本」没有单独的常量文件，靠下面四处对齐；**权威来源是 `config.example.json`**
（安装器显示版本就是读它，见 `tool/pack/installer_main._app_version()`）：

| 位置 | 值 | 谁在用 |
| --- | --- | --- |
| `config.example.json` → `wechat_bot_agent` | `AIpet/1.17.2` | 安装器标题里的版本号；用户首次运行的默认配置 |
| `wechat/ilink_client.py` → `DEFAULT_BOT_AGENT` | `AIpet/1.17.2` | 微信 iLink 通道的 bot agent 头 |
| `wechat/wechat_bridge.py` → `bot_agent=` 默认参数 | `AIpet/1.17.2` | 桥接默认值 |
| `run_wechat.py` → 兜底 `or "AIpet/1.17.2"` | `AIpet/1.17.2` | 配置里读不到时的兜底 |

⚠ **用户本机 `config.json` 没动**（它被 .gitignore 忽略、含密钥、属用户数据）：里面的
`wechat_bot_agent` 还是旧值 → 实际运行时报给微信的版本号仍是旧的。要同步改的话
说一声（一行改动，但那是用户数据，不擅自动）。

README「最新版本」章节新增 `V1.17.2` 条目，覆盖本次工作（§9~§14 的全部用户可见改动：
界面可读性两族修复、Live2D 三画布根因、AI 选表情动作、全角情绪标签、槽位与徽章、
模型名对齐、一批体验修复）。

### 15.2 「更新日志」功能从"没用"做成能用（用户交办）

用户原话：「首页『更新日志』功能现在没用，从 README 复制更新过来做好」。

改前的现实（两个入口，两种"找最新"的写法，都不可靠）：

| 入口 | 改前 | 毛病 |
| --- | --- | --- |
| 首页快捷入口 `SiliconLauncher.open_changelog` | `sorted(glob(更新日志/*), reverse=True)[0]` + `os.startfile` | **按文件名字典序** → `V1.8.0.md` 排在 `V1.16.1.md` 前面，点开的是旧版本；把 .md 甩给系统默认程序，没装阅读器只弹"打开方式" |
| 设置页「其他」→ 查看日志 | 按文件 **mtime** 取最新，自己弹一个写死配色的 QPlainTextEdit | 同批生成的文件 mtime 相同 → 谁"最新"看运气；且浅色主题下是硬编码浅底 |
| 两者共同的毛病 | 文件夹空 → 只弹"未找到更新日志文件" | —— |

现在：

1. **数据从 README 生成**：新增 `tool/gen_changelog.py`，解析 README「## 🆕 最新版本」章节的
   `**Vx** — 说明` + `### Vx 变更` 段落 → 写 `更新日志/V<版本>.md`（14 个版本，含
   `V1.10.x` 这种标题配 `**V1.10.2**` 的错位写法、以及「历史版本」摘要）。
   `--check` 只校验不改（自测用），`--print` 打印解析结果（排查用）。
   ⚠ 生成的文件**不要手改**：README 改完重跑脚本即可。
2. **一个新模块管两边** `pcl_launcher/changelog.py`：`entries()/latest()/read()/show()`，
   **按版本号**排序（1.16.1 > 1.8.0）；`show()` 是主题化阅读窗口：版本下拉可切换、
   `QTextDocument.setMarkdown()` 直接渲染 md（不可用时回退纯文本）、底部「打开目录 /
   用系统程序打开 / 关闭」；**目录为空时回退读 README 的版本历史**（源码版/绿色版都有 README，
   即使 `更新日志/` 没随包带上也能看），两边都没有才给可读提示。
3. 首页 `open_changelog` 与设置页 `_view_changelog` 都改成调 `changelog.show()`；
   设置页的「最新一篇」也走 `changelog.latest()`。
4. 打包：`更新日志/` 是 git 追踪目录 → 绿色版按 `git ls-files` 自动带上；
   另在 `build_launcher.py` 的"git 不可用兜底清单"里补了 `更新日志`。

### 15.3 源码版安装/启动说明贴合实际（用户交办）

用户原话：「现在的绿色版还是 1.16.1，所以 README 里绿色版的启动方式暂时不变，
而源码版的用户的安装与启动方式也需要贴合实际」。

核对现实发现的差距：

- `run.py`（桌宠入口）最顶部有"换到项目自带解释器 `runtime\venv`"的自切换；
  **`run_launcher.py`（图形启动器）没有** → 源码版用户按 README 敲 `python run_launcher.py`
  （系统 Python）会 `ModuleNotFoundError: PyQt5`（双击 .py 时更常见）。
- 桌宠/QQ/微信都有双击 bat，**图形启动器没有**。

改法（只动源码版那份体验，绿色版文档一字未改）：

1. `tool/paths.py` 新增 `project_python_path()` 与 `ensure_project_python()`（把 `run.py` 里
   那段逻辑抽成公共实现，冻结版/`AIPET_REEXEC=1`/已在项目解释器上三条短路；Windows 上
   不用 `os.execv`——实测换完会 segfault，改为拉子进程后本进程退出）；
   `run_launcher.py` 在 **import PyQt5 之前**调它。
2. 新增 **`启动启动器.bat`**（CRLF！cmd 的 if 块对 LF-only 会解析出错）：与 `启动桌宠.bat`
   同款结构，探针只查 **PyQt5**（启动器不 import torch，缺 torch 的 venv 照样能开启动器，
   不该因此降级到很可能连 PyQt5 都没有的系统 Python）。
3. README 四处源码版说明改成实物：精简易用教程启动表加一行「源码版 → `启动启动器.bat`」、
   手动安装第 6 步给出 `runtime\venv\Scripts\python.exe run_launcher.py`（并说明
   `python run_launcher.py` 也会自动换解释器）、绿色版 vs 开源版对比表的开源版一列、
   「PCL 启动器」「人脸识别使用指南」里的入口说明。
   **绿色版那几处保持原样**（Green 包还是 1.16.1）。

自测：新增 `test_source_startup.py`（自切换四条分支 + bat 实物/CRLF/探针 + README 与实物一致 +
绿色版说明未被动过）与 `test_changelog.py`（版本排序 / 与 README 同步 / 版本锚点 /
README 回退 / 两入口统一 / 真起一次窗口）。


## 16. 绿色版实测四问（用户 2026-09-24 打包后当场测出来的）

用户拿到 `dist/AIpet-Murasame` 后点开就发现 4 个问题。逐条查证如下 —— **没有一条是本次改动
弄坏的，但每条都要修**：

| # | 现象 | 真正原因 |
| --- | --- | --- |
| 1 | 点「启动桌宠」报"没找到 Python 解释器" | 包内没有 `runtime\venv`；而 exe（frozen）当时**不回落系统 Python** |
| 2 | 点「启动微信」报"未找到微信模块（run_wechat.py）" | `run_wechat.py`/`wechat/` **都在包里**；真因同 ① 的"没有解释器"，报错文案把两件事混成一句 |
| 3 | 默认角色是诺瓦不是丛雨 | `pets/pet_list.json` 的 `active` 写着 `noir`（`config.example.json` 无 `active_pet`，启动器读它） |
| 4 | 主题少一个 | `.gitignore` 把 `themes/classic/`（默认主题，**只有 795 B 的 theme.json、零素材**）和 `themes/senrenbanka_copy/`（作者自建主题）排除在仓库外；绿色版**按 git 清单**复制 → 包里只剩 2 个主题 |

### 16.1 纠正一处我自己的错误判断（"exe 故意不用系统 Python"）

我最初把"代码形状"当成了"设计意图"，说成"冻结版故意面向没装 Python 的用户"。这与事实不符：
README 的教程第一步就是装 Python 3.10，`install.bat` 是官方第一步。查证：

- `_find_python` 是整合提交 `2725311` 带进来的，**之后一次没改过**；
- 它原来只有 ①②两个候选，打包版直接 `return ""`；
- **但它自己的报错文案写的是"启动器需要 runtime\venv 或系统 Python"** —— 文案承认系统 Python 可用，代码却从不返回；
- 三个 bat（桌宠/QQ/微信）**都会**回落系统 Python（`set "PYTHON_CMD=python"`）并带依赖探针；
- 第二个候选 `<exe目录>\python.exe` 与安装器布局也对不上（安装器的便携解释器在 `app\python\python.exe`）。

⇒ 结论：**是漏了回落 + 文案与行为不一致**，不是设计。修法（用户拍板"加回落"）：

```python
PET_NEED = ("PyQt5", "torch")          # ← 与 启动桌宠.bat 的 find_spec 探针同一套口径
QQ_NEED  = ("websocket", "requests")   # ← 启动QQ.bat
WX_NEED  = ("requests", "Crypto", "qrcode")   # ← 启动微信.bat
_find_python(base, need):  ① runtime\venv ② 包内 python.exe
                           ③ 源码模式 → sys.executable
                           ④ 打包版 → PATH 上的 python/python3，**先过 _python_has() 依赖探针**
```
`_python_has()` 用 `importlib.util.find_spec` 只查不导入（毫秒级、不触发 DLL），
`CREATE_NO_WINDOW` 避免探测时闪黑框。探针不过就交给 `_require_python()` 报**准确**原因：
"系统里找到 Python 但缺依赖（需要 PyQt5、torch）→ 去跑 install.bat"，不再指向"缺文件"。

⚠ 这个回落只对"系统 Python 里已经装好依赖"的机器有效（开发机立刻就能用）；
**全新用户的机器仍然要跑一次 `install.bat`**，否则探针会拦下并给出指引 —— 它不是"免安装"的替代品。
真要"解压即用、不需要 Python"，那是**一键安装器**那条路（载荷里带 `app/runtime/venv` + `app/python/`，
安装时改写 `pyvenv.cfg` 指向随包 python），代价是包 +约 1.8 GB。

### 16.2 另外三处改动

1. `_require_python(what, need)` 统一三条入口（原来各抄一份），并把"缺解释器"和"缺文件"分开报：
   微信入口现在先报运行环境、再单独报 `run_wechat.py` 缺不缺（QQ 同理加了文件检查）。
2. `pets/pet_list.json`：`active` → `murasame`（出厂默认角色 = 丛雨）。
3. `.gitignore`：放开 `themes/classic/` 与 `themes/senrenbanka_copy/`，**只**排除作者那份主题里的
   壁纸视频 `senrenbanka_copy/assets/bg_custom.mp4`（74.5 MB 自用素材，按用户要求"只带主题不带视频"）。
   ⚠ 该主题 `theme.json` 里 `background` 声明的正是这个视频 → 协作者拿到的这份主题是**无壁纸**版本
   （`background_info()` 找不到文件时优雅降级），要换成静态 `assets/bg.jpg` 得改 theme.json。

### 16.3 新增自测：`test_pack_contents.py`

盯住这次的 bug 类型（**本地有、包里没有**这类静默丢失）：

1. `pcl_launcher/themes/<id>/` 每个目录必须**要么被 git 追踪、要么在 .gitignore 里且写进白名单**
   （不写白名单就报警），并检查每个主题都有 `theme.json`；
2. 三个入口必须走 `_require_python()`，微信/QQ 的"缺文件"提示必须排在运行环境校验**之后**；
3. `_find_python` 必须有系统 Python 回落 + 依赖探针，且**探针口径与三个 bat 的 `find_spec` 一致**
   （两处写同一件事最容易走偏）；
4. `pet_list.json` 的 `active` 必须是真实存在、且为 `murasame` 的角色包；
5. `build_launcher.py` 仍按 `git ls-files` 复制（上面所有结论的前提）。


## 17. 绿色版实测第二批：桌宠"卡退"与微信"秒卡退"（两个互不相干的真 bug）

用户拿到重打的绿色版再测，报"启动到 CUDA 检测那行就卡退，微信秒卡退"。查证结果：
**两条都是真 bug，而且与打包无关**（源码树里同样复现），根因完全不同。

### 17.1 桌宠"卡退" = PyQt5 自带的旧 MSVC 运行时 vs 新版 torch（0xC0000005）

**证据链**（没有靠猜）：

1. 用户日志停在 `[AIpet] 检测到 CUDA 环境: 驱动 591.91，CUDA 13.1`，无 traceback；
2. Windows 事件日志（19:11 / 20:03 / 20:10 / 20:12 共 4 次，与测试时间吻合）：
   ```
   出错应用程序： python.EXE 3.10.4
   出错模块：     MSVCP140.dll 14.26.28720.3
   异常代码：     0xc0000005，偏移每次都是 0x12ea0
   Faulting 模块路径： …\site-packages\PyQt5\Qt5\bin\MSVCP140.dll
   ```
3. 无头复现（`python -c`，同样的解释器）：
   ```
   只 import PyQt5          → OK
   只 import torch          → OK (2.11.0+cu128)
   先 PyQt5 再 torch        → 崩（exit=1，OSError WinError 1114 loading torch\lib\c10.dll）
   先 torch 再 PyQt5        → OK
   ```
   项目 `.venv` 里同样崩（torch 2.12.0）。
4. 版本对照：PyQt5-Qt5 **5.15.2** 在 `PyQt5\Qt5\bin` 里带了一整套 **14.26（VS2017）** 运行时；
   系统 `System32` 的是 **14.51（VS2022）**；torch ≥2.x 的 `c10.dll` 需要新的。
5. 崩溃点精确到行：`run.py` 第 208 行 `import PyQt5.QtCore`（依赖自检）先把旧运行时
   拉进进程，第 474 行才 `import torch` → 必崩。**`main.py` 本来就是 torch 先、Qt 后**
   （注释里写着"本地模式要求在任何第三方库之前导入"），所以只有 `run.py` 这条路必崩；
   启动器只有 Qt，所以它一直没事。

**修法**（新增 `tool/msvc_runtime.py`）：
把 `PyQt5\Qt5\bin` 里那 6 个**旧于系统**的文件
（`msvcp140 / msvcp140_1 / msvcp140_2 / vcruntime140 / vcruntime140_1 / concrt140`）
改名成 `*.aipet-disabled` 让位，Windows 就会用系统的新运行时。
护栏：只动"系统确实有更新版本"的那一个文件（否则 Qt 可能缺依赖）；幂等；纯改名、可回滚；
Qt 自己的 DLL（Qt5Core.dll 等）绝不碰。接线：
- `run.py`：在**第一次 import PyQt5 之前**调用（否则旧运行时已经进进程了）；
- `install.bat`：装完依赖多一步 `[4.5/5]` 调用它（新装的 venv 一次到位）；
- 手动：`python -m tool.msvc_runtime [--dry-run]`。

验证：改名后 `import PyQt5.QtWidgets` + 建窗口渲染 + `import torch` + `import cv2` 同进程 exit=0
（系统 Python 与 `.venv` 都已就地修好）。

⚠ 注意：这不是"打包引入的问题"，而是**PyQt5-Qt5 5.15.2 + 新版 torch 的版本冲突**，
只要一个环境里同时有这两者、且 Qt 先加载的人都会中招（用户的开发机也一样）。

### 17.2 微信"秒卡退" = `get_config()` 遇到缺失的 config.json 直接抛异常

复现（在包目录里直接跑）：`run_wechat.py` 第 249 行 `cfg = get_config("./config.json")`
→ `tool/config.py` 是**裸 open()** → `FileNotFoundError` → 进程立刻退出（控制台一闪就没，
看着像"崩溃"）。绿色版**故意不带** config.json（隐私：里面是密钥），所以这个入口必崩。

而且 README 写着「首次打开 PCL 会自动从 config.example.json 生成一份空白 config.json」——
**代码里根本没有这段逻辑**（只有 `install.bat` 与一键安装器会生成）。两处一起修：

1. `tool/config.py`：
   - `get_config()` 改为**缺失/损坏时回退到同目录 config.example.json 的默认值**并打印一次提示。
     用示例而不是空 dict 是故意的：调用方大量 `get_config(...)["model_type"]` 这样**下标访问**，
     返回空 dict 只会把 `FileNotFoundError` 换成 `KeyError`（已核对：5 个下标键在示例里都在）。
   - 新增 `ensure_config(path)`：首次运行由示例生成空白 config.json，**已存在绝不覆盖**。
2. 接线：`silicon_window.launch()` 与 `run.py` 启动时各调一次 `ensure_config`（补齐 README 的承诺）。
3. `start_wechat`：拉起进程**之前**先看 `wechat_enabled` —— 没开就给主题提示并告诉他去
   「设置 → 微信」开启（以前会拉起进程、进程打印"未启用"后秒退，控制台一闪，用户以为崩了）。

### 17.3 新增/加固的自测

```
test_msvc_runtime.py   只让旧版让位 / 幂等 / 三条护栏（系统更旧、缺同名文件、读不到版本）/
                       run.py 顺序（必须早于第一次 import PyQt5）/ 本机冲突已消除
test_pack_contents.py 追加：缺 config.json 时 get_config 回退且下标键齐全、
                       ensure_config 建一次且不覆盖、启动器与桌宠入口都接了它、
                       点微信前先看开关
probe_msvc_import_order / probe_config_fallback / probe_ensure_config
                       一次性探针（复现崩溃顺序、验证回退与生成语义）
```





