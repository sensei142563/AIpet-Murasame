# NapCat / QQ 版本锚（本项目的 NapCat 已锁定，请勿更新）
#
# 为什么锁：NapCat 自身带一个更新器（WebUI 里的「更新」按钮会去 GitHub 拉
#   releases/latest 并覆盖 NapCat.Shell.zip），所以版本要钉住，别让它在用户机器上乱跑。
#
# 2026-09-24 升级记录（4.18.14 → 4.18.28）：
#   原因：QQ 自己升到了 9.9.35-52892，而 4.18.14 的包体支持表上限是 9.9.32-50969
#   → NapCat 报「PacketBackend 不支持当前QQ版本架构」：WS 能连上，但消息处理不可用。
#   4.18.28 的支持表**包含 9.9.35-52892**（表内共 74 项，最高就是它；官方说明：
#   QQ 使用 build 40768 及以上即可），所以把 NapCat 升到 4.18.28、QQ 保持 9.9.35-52892。
#   移植方式：只覆盖内容不同的文件（31 个改动 + 92 个新增），
#   **保留 config/ cache/ logs/ guild1.db**（账号、onebot11 网络配置、WebUI 端口与 token）。
#   升级前整份安装的备份在：E:\university\order\python\pet\_backup_napcat_4.18.14\
#
# 谁读这个文件：
#   · build_launcher.py   —— 打包绿色版前比对本机 NapCat 实际版本，不一致就大声警告
#   · tool/pack/verify_installer.py —— 校验装出来的包里的版本对不对
#   · README「准备 NapCat」章节 —— 手动安装的用户按这里的版本下载
#
# 格式：每行 key=value，# 开头是注释。版本值必须与 NapCat/napcat.mjs 内的一致。

napcat=4.18.28
qq=9.9.35-52892
qq_max_supported=9.9.35-52892
